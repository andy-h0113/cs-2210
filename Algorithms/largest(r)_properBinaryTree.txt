largest(r)
In: root r of a proper binary tree
Out: Largest value stored in the leaves

if r is a leaf then return r.getValue()
else {
	v1 = largest(r.leftChild)
	v2 = largest(r.rightChild)
	if v1 > v2 then return v1
	else return v2
}